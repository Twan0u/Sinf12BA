typedef struct node{
	int value;
	struct node *next;
} node;

//Écrivez le corps de la fonction size_t length(node *list), qui doit renvoyer le nombre d’éléments se trouvant dans la liste.
 size_t length(node *list){
	if (list == NULL){return 0;}
	else{
		node current = &list;
		int count = 0;
		while (next != NULL){
			current = next
			count++;			
		}	 	
	}
}
